#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

vector<ll> a;

void solve(){
    ll n, k;

    a.resize(n + 1);

    for (int i = 1; i <= n; i++)
        cin >> a[i];

    
}

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;

    while(t-- > 0){
        solve();
    }
}